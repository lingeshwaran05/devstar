generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Paste {
  id        Int      @id @default(autoincrement())
  content   String
  name      String
  createdAt DateTime @default(now())
  expiresAt DateTime
}
